/**
 * Includes
 */

let numbers = [1, 2, 4, 6, 8, 9, 0];

if (numbers.includes(7)) {
  console.log('Si se encuentra el valor');
} else {
  console.log('No se encuentra');
}

/**
 * Operaciones exponenciales
 */

let base = 4;
let exponente = 3;
let resultado = base ** exponente;

console.log(resultado);